@page "/AddNewUser"
@using Employee_Crud_Blazor.Data
@inject UsersService employeeService
@inject NavigationManager NavigationManager

<h3>Add New User</h3>
<hr />

<form>
    <div class="row">
        <div class="col-md-8">
            <div class="for-group">
                <label for="FirstName" class="control-label">First Name</label>
                <input form="FirstName" class="form-control" @bind="@obj.FirstName" />
            </div>            
            <div class="for-group">
                <label for="LastName" class="control-label">Last Name</label>
                <input form="LastName" class="form-control" @bind="@obj.LastName" />
            </div>
            <div class="for-group">
                <label for="Email" class="control-label">Email Address</label>
                <input form="EmailAddress" class="form-control" @bind="@obj.Email" />
            </div>
            <div class="for-group">
                <label for="Login" class="control-label">Login</label>
                <input form="Login" class="form-control" @bind="@obj.Login" />
            </div>
            <div class="for-group">
                <label for="Password" class="control-label">Password</label>
                <input form="Password" class="form-control" @bind="@obj.Password" />
            </div>
            <div class="for-group">
                <label for="Password2" class="control-label">Password2</label>
                <input form="Password2" class="form-control" @bind="@obj.Password2" />
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                <input type="button" class="btn btn-primary" @onclick="@CreateNewEmployee" value="Save Employee"/>
                <input type="button" class="btn btn-danger"  @onclick="@Cancel" value="Cancel"/>
            </div>
        </div>
    </div>
</form>

@code {
    Users obj = new Users();

    protected async void CreateNewEmployee()
    {
        obj.CreateDate = DateTime.Now;
        await employeeService.AddNewEmployee(obj);
        NavigationManager.NavigateTo("Users");
    }
    protected async void Cancel()
    {
        NavigationManager.NavigateTo("Users");
    }
}
